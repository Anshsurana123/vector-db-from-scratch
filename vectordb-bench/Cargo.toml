[package]
name = "vectordb-bench"
version = "0.1.0"
edition = "2021"

[dependencies]
vectordb-core = { path = "../vectordb-core" }
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
